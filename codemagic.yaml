workflows:
  flutter-app:
    name: Flutter App Workflow
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build apk
    artifacts:
      - build/**/outputs/**/*.apk